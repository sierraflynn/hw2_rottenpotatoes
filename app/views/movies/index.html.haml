-#  This file is app/views/movies/index.html.haml
-# http://haml.info/tutorial.html at adding attributes
-#anything want to add to path goes into "params"
%h1 All Movies

%table#movies
  %thead
    %tr
      %th{:class => @titlestyle}= link_to "Movie Title", movies_path(:order => "movies.title"), :id => "title_header"
      %th Rating
      %th{:class => @datestyle}= link_to "Release Date", movies_path(:order => "movies.release_date"), :id => "title_header"
      %th More Info
  %tbody
    - @movies.each do |movie|
      %tr
        %td= movie.title 
        %td= movie.rating
        %td= movie.release_date
        %td= link_to "More about #{movie.title}", movie_path(movie)

= link_to 'Add new movie', new_movie_path
